import MapaPantalla from "@/components/MapaPantalla";

export const metadata = {
  title: "ATLAS SENTINEL — Mapa",
  description: "El mapa de riesgo de la Ciudad a pantalla completa, con sus filtros y su contexto.",
};

export default function PaginaMapa() {
  return <MapaPantalla />;
}
